# PROJECT_SETUP.md

# Status: [INCOMPLETE — Claude must ask for all [NOT SET] fields before starting]

---

## Project

| Field                    | Value     |
| ------------------------ | --------- |
| Project name             | [NOT SET] |
| Short description        | [NOT SET] |
| Primary content language | [NOT SET] |
| Figma file URL           | [NOT SET] |

---

## Typography

| Field            | Value                                                   |
| ---------------- | ------------------------------------------------------- |
| Sans-serif font  | [NOT SET]                                               |
| Font source      | [NOT SET — Google Fonts URL / local file / npm package] |
| Fallback font    | [NOT SET]                                               |
| Monospace font   | [NOT SET — or "none"]                                   |
| Monospace source | [NOT SET — or "none"]                                   |

---

## Color modes

| Field                  | Value                                                         |
| ---------------------- | ------------------------------------------------------------- |
| Modes required         | [NOT SET — "light only" / "light + dark" / other]             |
| Dark mode token source | [NOT SET — "separate Figma page" / "auto-generated" / "none"] |

---

## Integrations

| Service                | Value                                                |
| ---------------------- | ---------------------------------------------------- |
| Version control        | [NOT SET — GitHub / GitLab / Bitbucket / none]       |
| Repository URL         | [NOT SET — or "none"]                                |
| Project management     | [NOT SET — Jira / Notion / Linear / none]            |
| Project management URL | [NOT SET — or "none"]                                |
| Backend / database     | [NOT SET — Firebase / Supabase / custom API / none]  |
| Authentication         | [NOT SET — NextAuth / Firebase Auth / custom / none] |
| Analytics              | [NOT SET — GA4 / Plausible / none]                   |
| Other integrations     | [NOT SET — or "none"]                                |

---

## Security and environment variables

| Field                               | Value                                               |
| ----------------------------------- | --------------------------------------------------- |
| Environment variables needed        | [NOT SET — list all expected .env keys]             |
| Client-side variables (public)      | [NOT SET — list which vars are safe to expose]      |
| Secret variables (server-side only) | [NOT SET — list which must stay server-side]        |
| Secrets management                  | [NOT SET — .env.local / deployment platform / none] |

---

## Deployment

| Field             | Value                                                  |
| ----------------- | ------------------------------------------------------ |
| Deployment target | [NOT SET — Vercel / Netlify / custom server / unknown] |
| Production URL    | [NOT SET — or "not yet known"]                         |
| Staging URL       | [NOT SET — or "none"]                                  |
| CI/CD pipeline    | [NOT SET — GitHub Actions / other / none]              |

---

## Production readiness

| Field          | Value                                          |
| -------------- | ---------------------------------------------- |
| Favicon added  | [NOT SET — yes / no / skipped until next push] |
| App icon added | [NOT SET — yes / no / skipped until next push] |
| OG image added | [NOT SET — yes / no / skipped until next push] |

---

## Notes
